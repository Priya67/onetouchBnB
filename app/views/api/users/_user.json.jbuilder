json.extract! user, :id, :username, :fname, :lname, :email, :phone_number, :img_url

# json.array! user.spots do |spot|
#   json.extract! spot, :id, :headline, :description,
#                       :img_url, :rating, :price, :room_type,
#                       :lat, :lng, :city, :location
# end
